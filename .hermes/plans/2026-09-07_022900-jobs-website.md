# Jobs Website Plan (STATIC — Supabase removed Sep 7)

**Goal:** Browse ALL currently-eligible jobs on a friends-usable site. No database, no keys, no secrets anywhere.

**Architecture:** Each Actions run scans (halves) → `export_site.py` converts SQLite to `site/jobs.json` (307KB) + `site/meta.json` → pushed to public `Rishith-S/jobs-board` → Cloudflare Pages serves + single `index.html` filters client-side. PROVEN locally Sep 7: renders 985 roles, banner correct, search verified (`openai` → 174, matches DB).

**Tech Stack:** Existing scanner + static HTML/JS + Cloudflare Pages. Zero new services.

## Done (verified)

- [x] Exporter with honesty rules (listed = seen in latest complete snapshot + recomputed eligibility)
- [x] Page renders + filters, JS syntax-checked AND browser-driven against real data
- [x] Email fully removed; workflow needs zero secrets
- [x] 53 real boards, halves, ~1,100 min/mo one account

## Left

1. **You:** create public `Rishith-S/jobs-board`, connect to Cloudflare Pages (preset None).
2. **Me:** workflow step pushing `site/` there (deploy key), backfill push, live-URL check.
3. **Later, not now:** Adzuna aggregator source (needs your free dev keys in secrets), visa-sponsor labels (needs descriptions kept first).

## Risks

- Half-stale views: each company refreshes every ~4h; banner shows rebuild time + freshest board — never presented as live.
- Deleted roles vanish on next rebuild of their board (correct).
